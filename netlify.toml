[build]
  command = "npm install && npm run build"
  publish = "build"  # For CRA use 'build', for Next.js see note below

[functions]
  directory = "functions"

[[redirects]]
  from = "/send-email"
  to = "/.netlify/functions/send-email"
  status = 200
  methods = ["POST"]
