[build]
  command = "cd app && npm install && npm run build"
  publish = "raza/build"  # or appropriate output

[functions]
  directory = "functions"

[[redirects]]
  from = "/send-email"
  to = "/.netlify/functions/send-email"
  status = 200
  methods = ["POST"]
